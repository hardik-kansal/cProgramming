#include <bits/stdc++.h>
using namespace std;
int n, m;
const int N=200005;
vector<vector<int>> adj(N);

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> n;

    for(int i=0; i<n-1; i++){
        int a,b;cin>>a.>b;
        adj[a].push_back(b);
    }


}
